#include<stdio.h>
#include<stdlib.h>
int main(){
int  countUnsignedNumber;
int numberArray[countUnsignedNumber];
printf("nhap size cua mang\n");
scanf("%d",&countUnsignedNumber);
int i;
	    	for (i = 0; i < countUnsignedNumber; i++){
	    		printf("nhap gia tri cho index thu %d\n",i);
	    		scanf("%d",&numberArray[i]);
	    		printf("tong gia tri trong ham la %d+ %d = %d",numberArray[i],num,countUnsignedNumber);
	    		}
	    		for (i = 0; i < countUnsignedNumber; i++){
		   		printf("phan tu thu %d co gia tri la %d\n",i,numberArray[i]);
			}
	    	return 0;	
			}

